# 2026-07-04 工作日志

## Edge/Chrome MV3 长截图扩展
- 创建了完整的浏览器扩展项目 `extension/`，基于 `captureVisibleTab` + 自动滚动分段捕获 + OffscreenCanvas 拼接
- 核心文件：manifest.json, popup.html/js, background.js, stitch.js, scroll.js, style.css
- 修复了图标缺失问题（icon16/48/128.png），使用纯 Node.js 生成 PNG（紫色圆形）
